import React from 'react';
import './Header.scss';

const Header = () => {
  return (
    <header className="header-bar">
      <h1>
        React Todo
      </h1>
    </header>
  );
};

export default Header;